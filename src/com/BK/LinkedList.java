package com.BK;


public class LinkedList<E> {
    /*
    public int size;

    public LinkedList() {
        top = null;
        tail = null;
        size = 0;
    }//end LinkedList()

    public boolean isEmpty() {
        return top == null;
    }//end isEmpty()

    public void display() {
        if(size == 0) {
            System.out.println("Structure Empty");
            return;
        }
        if(top.getLinkNext() != null) {
            System.out.println(top.getData());
            return;
        }
        Node ptr = top;
        System.out.println(top.getData());
        ptr = top.getLinkNext();
        while(ptr.getLinkNext() == null) {
            System.out.println(ptr.getData());
            ptr = ptr.getLinkNext();
        }
        System.out.println(ptr.getData() + "\n");
    }//end display

*/
}//end class LinkedList